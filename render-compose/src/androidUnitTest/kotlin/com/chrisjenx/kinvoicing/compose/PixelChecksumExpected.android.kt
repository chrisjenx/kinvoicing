package com.chrisjenx.kinvoicing.compose

// BitmapFactory needs Robolectric or a device, neither configured here.
// The 0L sentinel skips the equality assertion; the test still prints
// the observed value for future baselining.
internal actual val expectedPixelChecksum: Long = 0L
