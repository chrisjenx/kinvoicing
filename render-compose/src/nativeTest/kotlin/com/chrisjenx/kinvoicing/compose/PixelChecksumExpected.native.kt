package com.chrisjenx.kinvoicing.compose

// Skiko native (iOS, macOS, Linux, Mingw — all share the Skiko native
// binary). Matches the JVM baseline exactly because both go through the
// same Skia C++ library compiled for native.
internal actual val expectedPixelChecksum: Long = -3520607678554403233L
